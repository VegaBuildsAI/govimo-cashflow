# Mock UI: decisiones que no están en el código

- Moneda base de reporte del mock: **USD** (docx dice "USD o CRC, a definir con Govimo") — pendiente confirmar.
- Los docx se extraen sin pandoc (no instalado): `python` + zipfile sobre `word/document.xml`. Extractos quedaron en `1/2/3-extract.txt` en la raíz.
- `preview_screenshot` del MCP Claude Preview se cuelga intermitentemente con esta app; verificar render con `preview_eval` (DOM/innerText) cuando pase. Nota: `innerText` devuelve texto ya transformado por CSS `uppercase` — comparar en lowercase.
- Sidebar usa `rounded-lg` en nav (no pill) a propósito: pill es para CTAs según la marca; nav clona el patrón del repo-actas.
